export enum UserMode {
    Default = 1,
    AddAdmin = 20,
    RemoveAdmin = 21
}

export enum UserStatus {
    Temp = 0,
    OK = 1,
    Blocked = 3,
    Banned = 4
}

export enum UserType {
    Default = 1,
    Admin = 2
}
